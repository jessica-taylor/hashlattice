data:
  headers:
    "Content-Type": "text/html"
  content: |
    <html><body><p>Here's another page!</p></body></html>
code: |
  function(hl) { 
    return {headers: headers, content: new Buffer(content, 'utf8')};
  }
