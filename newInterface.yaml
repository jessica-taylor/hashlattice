data:
  headers:
    whatever
  varhash:
    !hashYaml myvar.yaml
code: |
  function(hl) {
    getHash, evalComputation
    hl.getVar(v)
    hl.getPath()
    hl.putHash()
    hl.putVar()
    function putDataIntoFacebook(content) {
      return hl.putVar(AAAA, content)
    }
    return {
      headers: headers,
      api: {
        putDataIntoFacebook: putDataIntoFacebook
      },
      content: hl.getVar(varhash).content
    }
  }
